# =============================================================================
# BETANCOURT AUDIO - ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# DATABASE_URL is used by Prisma (OAuth system)
DATABASE_URL=postgresql://postgres:postgres@db:5432/betancourt_audio

# PostgreSQL configuration (used by Django)
POSTGRES_DB=betancourt_audio
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# DJANGO CONFIGURATION
# -----------------------------------------------------------------------------
SECRET_KEY=django-insecure-dev-key-change-in-production-please
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend,frontend

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://frontend:3000

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION
# -----------------------------------------------------------------------------
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# AUTH.JS (NextAuth.js v5) CONFIGURATION
# -----------------------------------------------------------------------------
# Generate AUTH_SECRET with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here-generate-with-openssl
NEXTAUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# GOOGLE OAUTH PROVIDER
# -----------------------------------------------------------------------------
# Get credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# 3. Copy Client ID and Client Secret below
AUTH_GOOGLE_ID=your-google-client-id
AUTH_GOOGLE_SECRET=your-google-client-secret

# -----------------------------------------------------------------------------
# FACEBOOK OAUTH PROVIDER
# -----------------------------------------------------------------------------
# Get credentials from: https://developers.facebook.com/apps
# 1. Create a new app or use existing
# 2. Add "Facebook Login" product
# 3. Configure OAuth Redirect URIs: http://localhost:3000/api/auth/callback/facebook
# 4. Copy App ID and App Secret below
AUTH_FACEBOOK_ID=your-facebook-app-id
AUTH_FACEBOOK_SECRET=your-facebook-app-secret

# -----------------------------------------------------------------------------
# APPLE OAUTH PROVIDER
# -----------------------------------------------------------------------------
# Get credentials from: https://developer.apple.com/account/resources
# 1. Create Service ID
# 2. Enable "Sign in with Apple"
# 3. Configure Return URLs: http://localhost:3000/api/auth/callback/apple
# 4. Generate and download private key (.p8 file)
# 5. Copy Service ID and convert .p8 key to string
AUTH_APPLE_ID=your-apple-service-id
AUTH_APPLE_SECRET=your-apple-private-key-as-string

# -----------------------------------------------------------------------------
# MICROSOFT ENTRA ID (Azure AD) OAUTH PROVIDER
# -----------------------------------------------------------------------------
# Get credentials from: https://portal.azure.com/#view/Microsoft_AAD_IAM
# 1. Register application in Azure AD
# 2. Add Redirect URI: http://localhost:3000/api/auth/callback/microsoft-entra-id
# 3. Create client secret
# 4. Copy Application (client) ID and client secret below
AUTH_MICROSOFT_ENTRA_ID_ID=your-microsoft-client-id
AUTH_MICROSOFT_ENTRA_ID_SECRET=your-microsoft-client-secret
# Tenant ID: use 'common' for multi-tenant, or your specific tenant ID
AUTH_MICROSOFT_ENTRA_ID_TENANT_ID=common

# -----------------------------------------------------------------------------
# EMAIL SERVICE (Optional)
# -----------------------------------------------------------------------------
# Resend API for password reset emails and notifications
# RESEND_API_KEY=re_...

# -----------------------------------------------------------------------------
# PAYMENT GATEWAYS (Optional - for development/production)
# -----------------------------------------------------------------------------
# Stripe (USD payments)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_SECRET_KEY=sk_test_...

# Bold (COP payments)
# NEXT_PUBLIC_BOLD_PUBLIC_KEY=...
# BOLD_SECRET_KEY=...

# -----------------------------------------------------------------------------
# LINEAR INTEGRATION (for issue management)
# -----------------------------------------------------------------------------
# Get your API key from: https://linear.app/settings/api
# LINEAR_API_KEY=your-linear-api-key-here

# -----------------------------------------------------------------------------
# PRODUCTION ENVIRONMENT OVERRIDES
# -----------------------------------------------------------------------------
# When deploying to production:
# 1. Set DEBUG=False
# 2. Generate new SECRET_KEY and AUTH_SECRET
# 3. Update NEXTAUTH_URL to your production domain (e.g., https://yourdomain.com)
# 4. Update all OAuth callback URLs in provider consoles to production URLs
# 5. Set strong POSTGRES_PASSWORD
# 6. Update ALLOWED_HOSTS and CORS_ALLOWED_ORIGINS
# =============================================================================
